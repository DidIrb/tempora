{
  "name": "tempora",
  "version": "1.0.0",
  "private": true,
  "description": "Tempora monorepo root",
  "scripts": {
    "prepare": "husky",
    "registry:build": "tsx scripts/buildRegistry.ts",
    "build": "npm run registry:build && npm --prefix packages/cli run build && npm run docs:sync",
    "dev": "npm --prefix packages/cli run dev",
    "lint": "npm --prefix packages/cli run lint",
    "docs:sync": "tsx scripts/syncDocs.ts",
    "docs:dev": "npm --prefix apps/docs run dev",
    "docs:build": "npm run docs:sync && npm --prefix apps/docs run build",
    "commit": "git-cz"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.0.0",
    "@commitlint/config-conventional": "^19.0.0",
    "commitizen": "^4.3.0",
    "cz-conventional-changelog": "^3.3.0",
    "husky": "^9.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.4.0"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  },
  "commitlint": {
    "extends": ["@commitlint/config-conventional"]
  }
}
